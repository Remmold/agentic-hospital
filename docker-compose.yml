services:
  hospital-simulation:
    image: nginx:alpine
    container_name: hospital-simulation
    restart: unless-stopped
    ports:
      - "8083:80"
    volumes:
      - ./src/hs_pipeline/visualization:/usr/share/nginx/html:ro
      - ./deploy/nginx-site.conf:/etc/nginx/conf.d/default.conf:ro
